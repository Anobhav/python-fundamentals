# change the values in a list which is inside a set 

s = {8,7,1, "harry", [1,2]}

# not possible becz we cant include a list inside a set because all elements inside a set should be immutable and hashable

 